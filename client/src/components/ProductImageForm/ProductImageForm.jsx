import React, { useState } from 'react';
import { uploadProductImages } from '../../services/uploadService/uploadService';


const ProductImageForm = ({ seriesId, productId }) => {


  return (
    <form onSubmit={handleSubmit}>
    
    </form>
  );
};

export default ProductImageForm;
