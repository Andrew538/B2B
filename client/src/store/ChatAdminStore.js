import { makeAutoObservable, runInAction, computed } from "mobx";
import { $api } from "../services";
import ChatServiceManager from "../services/chatService/chatServiceManager";


class ChatAdminStore {
 
}

export const chatAdminStore = new ChatAdminStore();
