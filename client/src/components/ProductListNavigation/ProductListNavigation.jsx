import { observer } from "mobx-react-lite";
import { useSearchParams } from "react-router-dom";
import classes from './productListnavigation.module.scss'
import BrandSelector from "../BrandSelector/BrandSelector";
const ProductListNavigation = observer(({ store, userGroupId }) => {
  

});


export default ProductListNavigation