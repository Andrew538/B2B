import React, { useState } from 'react';
import { observer } from 'mobx-react-lite';
import { chatAdminStore } from '../../../store/ChatAdminStore';


export const CreateGroupModal = observer(({ onClose }) => {

  return (
    <div >
     
    </div>
  );
});