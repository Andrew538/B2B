import { observer } from 'mobx-react-lite';
import React, { useEffect, useRef } from 'react';


export const MessageListAdmin = observer(({ messages, currentUserId }) => {


  return (
    <div >
     
    </div>
  );
});
